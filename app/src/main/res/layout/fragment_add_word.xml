<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".fragments.AddWordFragment">

    <data>
        <variable
            name="viewModel"
            type="com.cardsapp.cards.viewmodel.AddWordViewModel" />
    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textView"
            android:layout_width="match_parent"
            android:layout_height="73dp"
            android:text="TextView"
            tools:text="Add words" />

        <RadioGroup
            android:id="@+id/genderRadioGroup"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:checkedButton="@={viewModel.selectedRadioValue}">

            <RadioButton
                android:id="@+id/radioButtonDer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="DER" />

            <RadioButton
                android:id="@+id/radioButtonDie"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="DIE" />

            <RadioButton
                android:id="@+id/radioButtonDas"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="DAS" />

        </RadioGroup>

        <EditText
            android:id="@+id/enterGermanNoun"
            android:layout_width="match_parent"
            android:layout_height="104dp"
            android:hint="Enter german"
            android:text="@={viewModel.newGermanNoun}"/>

        <EditText
            android:id="@+id/enterRussianNoun"
            android:layout_width="match_parent"
            android:layout_height="113dp"
            android:hint="Enter Russian"
            android:text="@={viewModel.newRussianNoun}"/>

        <Button
            android:id="@+id/save_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{() -> viewModel.addPair()}"
            android:text="Save" />

    </LinearLayout>
</layout>